packages:
    default:
        actions:
            imgrec:
                function: __main__.py
                inputs:
                    image:
                        type: string
                        default: "1Mb.JPEG"
                        description: "Path to image to resize"
                    ipv4:
                        type: string
                        descrition: "local ipv4 device for swift connection"
                docker: onanad/action-python-v3.9:imgrec
                limits:
                    memorySize: 1024
                

